package com.mtislab.celvo.marketing.domain

enum class BannerType {
    HERO,       // Big main banner (Full width card with image on the right)
    SECONDARY   // Smaller square banner (Nomad style buttons)
}