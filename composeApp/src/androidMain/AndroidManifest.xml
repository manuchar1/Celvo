<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:name=".CelvoApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar"
        android:usesCleartextTraffic="true">

        <activity
            android:exported="true"
            android:name=".MainActivity"
            android:launchMode="singleTask"> <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>

            <!--
                Payment result deep link.

                Flow: User pays in BOG → BOG redirects to
                      https://api.celvoapp.com/payment/result/{success|fail}
                    → Android intercepts this URL before it reaches the server
                    → App opens via onNewIntent → DeepLinkHandler
                    → PendingPaymentStore provides the cached orderId
                    → Navigates to PaymentVerification screen

                The orderId is cached in PendingPaymentStore BEFORE the browser
                opens, so we don't need it from the URL.
            -->
            <!-- HTTPS App Link for api.celvoapp.com (Android intercepts BOG redirect) -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="https" />
                <data android:host="api.celvoapp.com" />
                <data android:pathPrefix="/payment/result" />
            </intent-filter>

            <!-- Custom scheme fallback (used by iOS and server HTML redirect page) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="com.mtislab.celvo" />
                <data android:host="payment" />
                <data android:pathPrefix="/result" />
            </intent-filter>

        </activity>

        <meta-data
            android:name="com.google.android.gms.wallet.api.enabled"
            android:value="true" />

    </application>

</manifest>